<div class="card__{{size}}">
    {{> '@card_image' }}
    <p class="card__text">{{text}}</p>
    <p class="card__title">{{title}}</p>
    <p class="card__text">{{text}}</p>
    <div class="card__overlay"></div>
    {{ render '@card-button' }}
</div>